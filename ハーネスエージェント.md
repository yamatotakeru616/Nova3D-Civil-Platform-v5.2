# Nova3D Civil Platform v5.2 - ハーネスエージェント.md
## 環境接続・検証ブリッジレイヤー設計書

本ドキュメントは、Nova3D Civil Platformが動作するクライアント環境（Windows 11 / NVIDIA GeForce RTX 3050 Ti Laptop GPU 4GB VRAM）および外部システム（OSファイルシステム、WebGPU、REST/RPC API、国交省BIM/CIMサーバー）と安全・高速に通信し、自己整合性を担保するための「ハーネス（Harness）レイヤー」の設計書である。

---

## 1. ハーネスアーキテクチャ概要

```text
+-------------------------------------------------------------------------------+
|                       Autonomous Civil Agent Core Loop                        |
+-------------------------------------------------------------------------------+
                                      |
                                      v
+-------------------------------------------------------------------------------+
|                       Nova3D Harness Verification Layer                       |
|  +--------------------+  +--------------------+  +-------------------------+  |
|  | Hardware & VRAM    |  | FileSystem Sandbox |  | MLIT Spec Invariant     |  |
|  | Monitor Bridge     |  | Safe I/O Bridge    |  | Assertion Guard         |  |
|  +--------------------+  +--------------------+  +-------------------------+  |
+-------------------------------------------------------------------------------+
         |                           |                           |
         v                           v                           v
+--------------------+      +--------------------+      +--------------------+
| GPU: RTX 3050 Ti   |      | Local Storage &    |      | 国交省標準フォーマット|
| 4GB VRAM Zero Leak |      | File System Export |      | LandXML / IFC 4.3  |
+--------------------+      +--------------------+      +--------------------+
```

---

## 2. 制限リソース保護（RTX 3050 Ti 4GB VRAM 最適化規約）

### 2.1 メモリ帯域＆VRAMリーク防止プロトコル
- **ジオメトリ破棄の徹底**:
  Three.js / WebGL / Canvas 2D において、視界外のコリドーメッシュ（Sta. 100m以遠）は `BufferGeometry.dispose()` および `Material.dispose()` を即座にトリガーし、常時VRAM使用量を **1.2GB以下** に抑制する。
- **テクスチャ解像度の動的スケーリング**:
  航空写真・PLATEAU 3D都市テクスチャは最大2048x2048に制限し、GPUメモリの溢れ（OOM: Out Of Memory）をハードウェア的に遮断する。
- **フレームレート監視ハーネス**:
  描画ループにおいて直近60フレームの移動平均FPSを監視。FPS < 55 を検知した場合、即座にLOD（Level of Detail）を一段階引き下げ、60FPS固定を維持する。
- **斜面安定工・間隙水圧解析のWebWorkerオフロード規約 [v5.3]**:
  修正フェレニウス法およびGreen-Ampt土壌浸透流計算（全測点×スライス法円弧すべり探索）は、メインスレッドを1ミリ秒もブロッキングさせないため、専用WebWorker（`slopePhysicsWorker.ts`）に完全分離。UIスレッドとのデータ転送は純粋なJSONオブジェクトとし、VRAM消費は11.4MB（RTX 3050 Ti 4GBの0.3%未満）に抑え、降雨時でも60FPS描画を完全死守する。

---

## 3. ファイルシステムI/O ＆ サンドボックス安全規約

### 3.1 成果物エクスポートの安全弁
- **ブラウザサンドボックス準拠**:
  生成された成果物（LandXML, IFC, OpenDRIVE, CSV）は、ブラウザネイティブの `Blob` 経由でメモリ上に生成され、`URL.createObjectURL()` により安全にダウンロードストリームへと橋渡しされる。
- **メモリ解放**:
  ダウンロード開始から1秒後に `URL.revokeObjectURL()` を強制実行し、ガベージコレクション（GC）を促進する。

---

## 4. 実行時自己検証（Invariant Assertion Guard）

すべてのエージェント操作（線形移動、土量計算、径間変更等）の前後に、以下の不変条件（Invariants）を自動テストするハーネスフックを常駐させる。

1. **ジオメトリ連続性保証**:
   $\lim_{s \to s_0^-} \vec{r}(s) = \lim_{s \to s_0^+} \vec{r}(s)$ （線形の $C^0$ および $C^1$ 連続性が寸断されていないこと）
2. **土量質量保存の法則**:
   $V_{\text{cut}} - V_{\text{fill}} + V_{\text{borrow}} - V_{\text{spoil}} = 0$ （盛土・切土・借土・残土の総量が厳密にゼロ収支であること）
3. **法規違反遮断**:
   道路構造令第15条（$R < 280\text{m}$）等の重大な安全基準違反が検知された場合、エージェントは自動コミットを中断し、ユーザーに警告ダイアログ（HITL: Human-In-The-Loop）を提示する。
